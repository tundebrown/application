import { FaFilePdf } from 'react-icons/fa';

const PDFIcon = () => {
  return (
    <div style={{ cursor: 'pointer' }}>
      <FaFilePdf size={32} />
    </div>
  );
};

export default PDFIcon;